# List of environment variables, which will be used if not passed explicitly to script.

# Git repo to use.
GITREPOSITORY=git://git.moodle.org/integration.git
GITREMOTE=integration
GITBRANCH=master

# Database used for moodle site.
DBHOST=localhost
DBTYPE=pgsql
DBNAME=moodle
DBUSER=moodle
DBPASS=moodle
DBPREFIX=mdl_
DBPORT=''

# Phpunit
PHPUNIT_DB_PREFIX="p_"

# Behat
BEHAT_DB_PREFIX="b_"
BEHAT_PROFILE=default

# Behat Process
BEHAT_RUN="" # Process number
BEHAT_TOTAL_RUNS="1" # Total number of processes

# Selenium.
SELENIUM_URLS=localhost:4444

# Save data in the shared folder with host.
SHARED_DIR=/shared
SERVER_DATA_DIR=/shared_data
